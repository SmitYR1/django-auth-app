[variables]
DJANGO_SETTINGS_MODULE = "django_auth_project.settings"

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn django_auth_project.wsgi --bind 0.0.0.0:$PORT"
